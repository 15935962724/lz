// Decompiled by Jad v1.5.7g. Copyright 2000 Pavel Kouznetsov.
// Jad home page: http://www.geocities.com/SiliconValley/Bridge/8617/jad.html
// Decompiler options: packimports(3)
// Source File Name:   UpdateTradeItem.java

package tea;

import java.io.PrintStream;
import tea.db.DbAdapter;

public class UpdateTradeItem
{

    public UpdateTradeItem()
    {
    }

    /*public static void main(String args[])
    {
        String s = "<A HREF=\"Node?Node=";
        String s1 = "<A HREF=\"Aded?Aded=";
        DbAdapter db = new DbAdapter();
        DbAdapter db1 = new DbAdapter();
        DbAdapter db2 = new DbAdapter();
        try
        {
            db.executeQuery("SELECT trade, time, subject, price, oquantity, squantity  FROM OldTradeItem0720  WHERE subject LIKE '<A HREF=%'  ORDER BY tradeitem ");
            while(db.next())
            {
                int i = db.getInt(1);
                java.util.Date date = db.getDate(2);
                String s2 = db.getString(3);
                java.math.BigDecimal bigdecimal = db.getBigDecimal(4, 2);
                int j = db.getInt(5);
                int k = db.getInt(6);
                int l = 0;
                if(s2.startsWith(s))
                    l = Integer.parseInt(s2.substring(s.length(), s2.indexOf(34, s.length())));
                else
                if(s2.startsWith(s1))
                    l = Integer.parseInt(s2.substring(s1.length(), s2.indexOf(34, s1.length())));
                if(l != 0)
                {
                    db1.executeUpdate("INSERT INTO TradeItem(trade, time, subject, price, oquantity, squantity)  VALUES(" + i + ", " + db1.cite(date) + ", " + l + ", " + bigdecimal + ", " + j + ", " + k + ") ");
                    System.err.print(l + "\r");
                }
            }
        }
        catch(Exception exception)
        {
            exception.printStackTrace();
        }
        finally
        {
            db.close();
            db1.close();
            db2.close();
        }
    }*/
}
