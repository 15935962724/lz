package tea.entity.node;


public class SearchField
{/*
    public static java.util.Enumeration getField(int type)
    {
        java.util.Vector vector = new java.util.Vector();
        tea.db.DbAdapter db = new tea.db.DbAdapter();
        try
        {
            db.executeQuery("sp_columns " + Node.NODE_TYPE[type]);
            while (db.next())
            {
                Field field = new Field();
                String name = db.getString(4);
                if (!name.equalsIgnoreCase("node") && !name.equalsIgnoreCase("listing") &&!name.equalsIgnoreCase("section")&&!name.equalsIgnoreCase("language"))
                {
                    field.setColumnName(name);
                    field.setColumnCapital(String.valueOf(name.charAt(0)).toUpperCase() + name.substring(1));
                    field.setTypeName(db.getString(6));
                    vector.addElement(field);
                }
            }
        } catch (Exception ex)
        {
            ex.printStackTrace();
        } finally
        {
            db.close();
        }
        return vector.elements();
    }
    public static java.util.Enumeration find(int type)
{
    java.util.Vector vector = new java.util.Vector();
    tea.db.DbAdapter db = new tea.db.DbAdapter();
    try
    {
        db.executeQuery("sp_columns " + Node.NODE_TYPE[type]);
        while (db.next())
        {
            Field field = new Field();
            String name = db.getString(4);
            if (!name.equalsIgnoreCase("node") && !name.equalsIgnoreCase("listing") &&!name.equalsIgnoreCase("section")&&!name.equalsIgnoreCase("language"))
            {
                field.setColumnName(name);
                field.setColumnCapital(String.valueOf(name.charAt(0)).toUpperCase() + name.substring(1));
                field.setTypeName(db.getString(6));
                vector.addElement(field);
            }
        }
    } catch (Exception ex)
    {
        ex.printStackTrace();
    } finally
    {
        db.close();
    }
    return vector.elements();
}*/

}
